#команда, которая показывает размер директории каждого пользователя
sudo du -sh /home/* | cut -d "/" -f 1,3 | tr -d "/" >> /home/titov/dev/userspace
#записываем ее в /etc/cron.daily, или в crontab -e с укзанием времени действия, можно ез sudo
